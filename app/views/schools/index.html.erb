
<h1>Listing Schools</h1>
<%= link_to 'New School', new_school_path %>
<div class="float_right"> <%= link_to "View All", :action => "index" %> | <a id="search" href="#search_form" rel="facebox">Advanced Search</a></div>
<div id="search_form" class="clear">
  <%= render "search" %>
</div>
<table>
  <tr>
    <th>School No</th>
    <th>School Name</th>
    <th>School Address</th>
    <th colspan="3">Action</th>
  </tr>

<% @schools.each do |school| %>
  <tr>
    <td><%=h school.school_no %></td>
    <td><%=h school.school_name %></td>
    <td><%=h school.school_address %></td>
    <td><%= link_to 'Show', school %></td>
    <td><%= link_to 'Edit', edit_school_path(school) %></td>
    <td><%= link_to 'Destroy', school, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<%= will_paginate @schools %>
