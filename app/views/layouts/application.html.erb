<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd"> 
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en"> 
	<head>
		<title>PCourse - <%=h yield(:title) %></title>
		<%= stylesheet_link_tag 'reset', 'grid', 'type', 'util', 'sprite' %>
		 <!--[if IE]>
		<%= stylesheet_link_tag 'ie' %>
		 <![endif]-->
		  <style>
		  .main{width:95%;}
		 </style>
		<%= stylesheet_link_tag 'scaffold', 'superfish', 'muffin', 'facebox', 'qkids' %>
		<%= javascript_include_tag 'jquery', 'hoverIntent', 'superfish', 'facebox', 'application' %>
		<%= yield(:head) %>
	</head>
	<body>
		<div id="header_container">
		<div class="main">
			<div id="header" class="hp">
				<h1>CourseName&reg; -- Location</h1>
				<div id="header_box">
					<% if current_user %>
						logged in as <%= current_user.username %> | <%= show_current_date %> | <%= link_to "Logout", logout_path %>
					<% end %>
				</div>
				<div class="clear"></div>
			</div>
		</div>
		<div class="clear"></div>
		</div>
		<div class="main">
			<% if current_user %>
				<%= render :partial => "shared/main_nav" %>
			<% end %>
			<% if flash[:notice] != nil %>
			<div id="flash"><div class="notice"> <span class="ss_sprite ss_lightbulb"></span>  <%= flash[:notice] %></div></div>
			<% end %>
			<div id="content" class="fluid">
				<%= yield %>
			</div>
			<br class="clear" />
			<div id="footer" class="hp">
				<hr />
				Created By Bobby Adi Prabowo
			</div>
		</div>
	</body>
</html>